import { css } from '@emotion/css';
import styled from '@emotion/styled';

export const StyledSettingsWrapper = styled.section({
  flex: 1,
  overflow: 'auto',
  padding: '20px 0',
  width: '100%',
});

export const overwriteInputClassName = css({
  minWidth: '120px',
});
